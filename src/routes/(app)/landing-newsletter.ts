/**
 * Buttondown newsletter the pre-launch landing signs visitors up to. Set at
 * build time (see README); when set, the landing's calls to action become a
 * subscribe form instead of "Start planning" / "Try the demo".
 */
export const NEWSLETTER_USERNAME = import.meta.env.VITE_BUTTONDOWN_USERNAME
