/**
 * Buttondown newsletter the pre-launch landing signs visitors up to. While
 * set, the landing's calls to action are a subscribe form instead of "Start
 * planning" / "Try the demo". Set to `undefined` at launch to bring them back.
 */
export const NEWSLETTER_USERNAME: string | undefined = 'kalkul'
